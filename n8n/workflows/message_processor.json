{
  "name": "Telegram Message Processor",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "message",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-message",
      "name": "Webhook: Message",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [220, 300]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "album",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-album",
      "name": "Webhook: Album",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [220, 600]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const crypto = require('crypto');\n\nconst webhookItem = $('Webhook: Message').first();\nconst headers = webhookItem.json.headers || {};\nconst body = webhookItem.json.body || {};\n\nconst secret = ($env.WEBHOOK_SECRET || '').trim();\nif (!secret) {\n  throw new Error('WEBHOOK_SECRET not set');\n}\n\nconst signature = (headers['x-signature'] || headers['X-Signature'] || '').toLowerCase();\nfunction stableStringify(value) {\n  if (value === null || typeof value !== 'object') return JSON.stringify(value);\n  if (Array.isArray(value)) return '[' + value.map((item) => stableStringify(item)).join(',') + ']';\n  return '{' + Object.keys(value).sort().map((key) => JSON.stringify(key) + ':' + stableStringify(value[key])).join(',') + '}';\n}\nconst payload = stableStringify(body);\nconst expected = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n\nif (!signature || signature.length !== 64) {\n  throw new Error('Missing or invalid X-Signature header');\n}\n\nconst sigBuf = Buffer.from(signature, 'hex');\nconst expBuf = Buffer.from(expected, 'hex');\nif (sigBuf.length !== expBuf.length || !crypto.timingSafeEqual(sigBuf, expBuf)) {\n  throw new Error('Invalid webhook signature');\n}\n\nreturn [{ json: body }];"
      },
      "id": "verify-hmac",
      "name": "Verify HMAC",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const crypto = require('crypto');\n\nconst webhookItem = $('Webhook: Album').first();\nconst headers = webhookItem.json.headers || {};\nconst body = webhookItem.json.body || {};\n\nconst secret = ($env.WEBHOOK_SECRET || '').trim();\nif (!secret) {\n  throw new Error('WEBHOOK_SECRET not set');\n}\n\nconst signature = (headers['x-signature'] || headers['X-Signature'] || '').toLowerCase();\nfunction stableStringify(value) {\n  if (value === null || typeof value !== 'object') return JSON.stringify(value);\n  if (Array.isArray(value)) return '[' + value.map((item) => stableStringify(item)).join(',') + ']';\n  return '{' + Object.keys(value).sort().map((key) => JSON.stringify(key) + ':' + stableStringify(value[key])).join(',') + '}';\n}\nconst payload = stableStringify(body);\nconst expected = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n\nif (!signature || signature.length !== 64) {\n  throw new Error('Missing or invalid X-Signature header');\n}\n\nconst sigBuf = Buffer.from(signature, 'hex');\nconst expBuf = Buffer.from(expected, 'hex');\nif (sigBuf.length !== expBuf.length || !crypto.timingSafeEqual(sigBuf, expBuf)) {\n  throw new Error('Invalid webhook signature');\n}\n\nreturn [{ json: body }];"
      },
      "id": "verify-hmac-album",
      "name": "Verify HMAC (Album)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 600]
    },
    {
      "parameters": {
        "functionCode": "// Word Replacement Engine — runs on single messages\nconst rules = $json.replacement_rules || [];\n\nfunction applyReplacements(text, rules) {\n  if (!text || typeof text !== 'string') return text;\n  let result = text;\n  for (const rule of rules) {\n    if (!rule || !rule.pattern) continue;\n    const pattern = rule.pattern;\n    const replacement = rule.replacement || '';\n    if (rule.is_regex) {\n      try {\n        result = result.replace(new RegExp(pattern, 'gm'), replacement);\n      } catch (e) {}\n    } else {\n      result = result.split(pattern).join(replacement);\n    }\n  }\n  return result;\n}\n\nconst data = {...$json};\nif (data.text) {\n  data.processed_text = applyReplacements(data.text, rules);\n  data.text_changed = data.processed_text !== data.text;\n}\nif (data.caption) {\n  data.processed_caption = applyReplacements(data.caption, rules);\n  data.caption_changed = data.processed_caption !== data.caption;\n}\nreturn [{json: data}];"
      },
      "id": "word-replacement",
      "name": "Word Replacement",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [700, 300]
    },
    {
      "parameters": {
        "functionCode": "// Word Replacement Engine — runs on album payloads\nconst rules = $json.replacement_rules || [];\n\nfunction applyReplacements(text, rules) {\n  if (!text || typeof text !== 'string') return text;\n  let result = text;\n  for (const rule of rules) {\n    if (!rule || !rule.pattern) continue;\n    const pattern = rule.pattern;\n    const replacement = rule.replacement || '';\n    if (rule.is_regex) {\n      try {\n        result = result.replace(new RegExp(pattern, 'gm'), replacement);\n      } catch (e) {}\n    } else {\n      result = result.split(pattern).join(replacement);\n    }\n  }\n  return result;\n}\n\nconst data = {...$json};\ndata.any_caption_changed = false;\nif (data.items && Array.isArray(data.items)) {\n  for (let item of data.items) {\n    if (item.caption) {\n      item.processed_caption = applyReplacements(item.caption, rules);\n      if (item.processed_caption !== item.caption) {\n        data.any_caption_changed = true;\n      }\n    }\n  }\n}\nreturn [{json: data}];"
      },
      "id": "word-replacement-album",
      "name": "Word Replacement (Album)",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [700, 600]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}"
      },
      "id": "respond-processed",
      "name": "Respond: Processed Message",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [940, 300]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}"
      },
      "id": "respond-processed-album",
      "name": "Respond: Processed Album",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [940, 600]
    }
  ],
  "connections": {
    "Webhook: Message": {
      "main": [
        [{ "node": "Verify HMAC", "type": "main", "index": 0 }]
      ]
    },
    "Webhook: Album": {
      "main": [
        [{ "node": "Verify HMAC (Album)", "type": "main", "index": 0 }]
      ]
    },
    "Verify HMAC": {
      "main": [
        [{ "node": "Word Replacement", "type": "main", "index": 0 }]
      ]
    },
    "Verify HMAC (Album)": {
      "main": [
        [{ "node": "Word Replacement (Album)", "type": "main", "index": 0 }]
      ]
    },
    "Word Replacement": {
      "main": [
        [{ "node": "Respond: Processed Message", "type": "main", "index": 0 }]
      ]
    },
    "Word Replacement (Album)": {
      "main": [
        [{ "node": "Respond: Processed Album", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
